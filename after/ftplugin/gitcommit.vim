if filereadable("$HOME/.vim/after/ftplugin/git.vim")
    source $HOME/.vim/after/ftplugin/git.vim
endif

" When on spell checking will be done.
setlocal spell

" When on, splitting a window will put the new window below the current one.
set splitbelow
